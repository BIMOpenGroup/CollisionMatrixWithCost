## Закрепление двух первых столбцов
- Добавить закрепление первых двух «данных» колонок (после «Группа» и «Элемент») и их заголовков при горизонтальном скролле.
- Реализация: в рендере обеих таблиц (`frontend/src/App.tsx`) помечать ячейки с абсолютными индексами колонок `0` и `1` дополнительными классами; в стилях (`frontend/src/App.css`) задать `position: sticky` и корректные `left`-смещения:
  - `row-group` (`left: 60px`) и `row-header` (`left: 60px`) уже закреплены; добавим `sticky-col-0` (`left: 260px`) и `sticky-col-1` (`left: 400px`) для `<th.col-header>` и `<td.cell>`.
- Локации правок: рендер матрицы 374–454 и 512–612; стили 28–115, 100–111.

## Перемещение строки источника
- Перенести `Источник: csv:/matrix.csv` под таблицу на вкладке «Матрица», чтобы панель действий не прыгала.
- Реализация: переместить блок `div.meta` из `frontend/src/App.tsx:376` ниже блока `.matrix-wrap` (перед `div.summary`).

## Правило: для `N/A` расчёты не выполняются
- Сервер: пропускать ячейки со значением `N/A` при задачах:
  - `compute_collisions_all` и `compute_risk_all` — загрузить CSV (`loadMatrixFromCsv`), проверять `matrix.grid[row][col] === 'N/A'` и пропускать.
  - Файлы: `backend/src/tasks.ts:94–167` и `169–193`.
- Интерфейс: на вкладке стоимости не открывать панель ячейки по клику, если исходное значение `N/A` (использовать `data.grid[ri][ciAbs]`), визуально оставить серый фон.

## Правило: для `Д` не считать сценарии, затраты = двойная стоимость материалов
- Сервер: для ячеек со значением `Д` не вызывать LLM и не генерировать сценарии.
- Вместо этого:
  - Получить принятые цены по элементам строки и колонки (`getAcceptedElementPrices`), отфильтровать категории «Материалы» (регистронезависимо, включает формы «материал», «материалы»).
  - Посчитать сумму цен `sumMaterials = sum(rowMaterials.price) + sum(colMaterials.price)` и записать `collision_costs.min = collision_costs.max = sumMaterials * 2`, `unit = 'RUB'`, `scenarios_json = '[]'`.
  - Локации: `backend/src/tasks.ts:94–167`.
- Интерфейс: скорректировать отображение на вкладке стоимости так, чтобы показывать `min/max`, даже если они равны (сейчас выводится только `Σ`), т.е. логика `display` в `frontend/src/App.tsx:564–571` — если есть `min` или `max`, показывать числовое значение.
- На вкладке стоимости не открывать панель ячейки по клику для `Д` (как и для `N/A`).

## Коррекция базовой матрицы CSV (зеркальная ось `Д`)
- В файле `frontend/public/matrix.csv` для каждой строки на диапазоне `/frontend/public/matrix.csv#L3-38` заменить все значения после первого `Д` на `N/A`.
- Реализация: одноразовое обновление файла (по правилу «после первого `Д` → все вправо `N/A`»). Дополнительно — защитная коррекция при загрузке: после `buildMatrixFromRecords(...)` выполнять такую же нормализацию данных в `frontend/src/data/loadMatrix.ts`, чтобы UI и сервер всегда видели корректную матрицу даже при будущих правках.

## Валидация
- UI: запустить фронтенд и вручную проверить горизонтальный скролл — два первых столбца зафиксированы на обеих вкладках; панель действий не смещается.
- Сервер: выполнить задачи и убедиться в логах, что `N/A` и `Д` пропускаются/обрабатываются по новым правилам; выборочно открыть панели ячеек для обычных значений.
- Тесты: добавить бэкенд‑тесты на пропуск `N/A` и калькуляцию `Д` (vitest), запускать в тихом режиме.

Готов приступить к реализации по этому плану.